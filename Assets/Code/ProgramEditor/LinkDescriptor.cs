using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LinkDescriptor : MonoBehaviour
{
    // References to the previous and next node of the link. Used in EditorProgram.FixLines() to get node shapes
    public GameObject prev, next;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
